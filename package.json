{
  "name": "spec-forge",
  "version": "0.1.0",
  "description": "Local-first CLI for spec and workflow artifact generation.",
  "bin": {
    "spec-forge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "diff": "^8.0.4",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
